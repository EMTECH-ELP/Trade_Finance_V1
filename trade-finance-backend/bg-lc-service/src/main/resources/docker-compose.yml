version: '3.3'
services:
  prometheus:
    image: 'prom/prometheus:v2.14.0'
    ports:
      - '9090:9090' # Prometheus is accessible on port 9090
    command: '--config.file=/etc/prometheus/config.yml'
    volumes:
      - './prometheus.yml:/etc/prometheus/config.yml'

  grafana:
    image: 'grafana/grafana:6.5.0'
    ports:
      - '8085:3000' # Grafana is accessible on port 8085
    environment:
      - GF_AUTH_DISABLE_LOGIN_FORM=true
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
    depends_on:
      - prometheus

#  my_application: # Replace this with your actual application service
#    image: <your-application-image>
#    ports:
      - '8085:8085' # Your application is accessible on port 8085
    # Add any other necessary configurations for your application here
